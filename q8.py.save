const smallestDivisor = (num) => {
  // BEGIN (write your solution here)
  if (num === 1){
    return 1;
  }
  const iter = (num, mindel) => {
    if(num % mindel !== 0 && mindel < num){
      return (num, mindel + 1);
    }
    else{
      return mindel;
    }
  }
  return iter(num, 2);
  // END
};






def smallestDivisor(num):
    if(num == 1):
        return 1


    def iter(num, mindel)
        if (num % mindel != 0 and num > mindel):
            return 
